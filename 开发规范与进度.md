# 开发规范与进度

> 目的:让新会话无需翻阅完整 `开发指导.md` 即可快速理解本仓库约定、已完成工作与下一步要做什么。新会话开始时请**先读本文件**,再按"下一步计划"接着做。
>
> 参考文档:[开发指导.md](开发指导.md)(权威设计文档,契约与公式以此为准)

---

## 一、项目定位

多智能体医疗会诊系统(论文第四章)。技术栈:**Streamlit + FastAPI + SQLite + FAISS**,推理内核为第三章微调后的 **MoE-LoRA + 困惑度驱动动态秩** 模型。阶段 0~6 使用 mock 模型跑通系统层,阶段 7 接入真实模型。

## 二、硬性开发规范(违反会破坏论文亮点,**必读**)

### R1. 一 4.3 节 一 目录/文件
`app/coordinator / app/router / app/agents / app/modes / app/aggregator / app/safety / app/knowledge / app/storage / app/model` 与论文 4.3.1~4.3.6 严格一一对应,**禁止合并**。答辩时需要"指着代码说这就是某一小节"。

### R2. 协调器是唯一大脑
- 只有 `ConsultationOrchestrator` 可以编排其他模块;router / agents / aggregator / safety **不得互相直接调用**。
- **数据库只由协调器读写**。其他模块不得直连 `app/storage/`。
- 违反会导致状态一致性崩盘(论文 4.3.1 原话)。

### R3. 模型层与系统层严格隔离
- `app/model/` 对外仅暴露 `ModelEngine.generate(prompt, rank_hint=None) -> (text, meta)`。
- `app/agents/` 只负责构造 prompt、解析输出,**绝不触碰模型内部**。
- 换基座模型时只需改 `app/model/`,上层零改动。

### R4. 两层专家是"软对应",不是强绑定
- 系统层的科室智能体**只能**通过 prompt 注入"科室名 + 推理重点"。
- **严禁**出现"请激活第 X 号专家"、"请使用科室 Y 的路由"这类指令。
- 模型层 `MoELoRALinear.layer_router` 的激活权重完全由隐状态决定,不接受外部科室标签控制。
- **这是第四章核心亮点,写错整章设计坍塌**。

### R5. 仲裁动作发起方必须是协调器
- `aggregator` 命中 L3 时通过 `coordinator_hook` 回调协调器。
- 协调器调用 `model.generate(prompt, rank_hint=max(rank_bins))` 获得仲裁稿。
- 代码里**必须**出现显式 `rank_hint=max(...)` 参数,否则"系统层与模型层协同"亮点落不了地。

### R6. 路由置信度公式
- 按 `dept` 分组 → 取该科室下相似度 **最大值**(**不是均值**!)→ softmax。
- 一致性检验用 **余弦最小值**(非均值),医疗场景对冲突零容忍。
- L2 加权中 `relevance` / `confidence` **直接读 `RoutingResult`,不要重算**;`completeness` 是五字段覆盖度线性扣分。

### R7. 会话 = 病例
一次 session 绑定一个病例,**禁止中途换病人**(极大简化状态管理)。

### R8. 异步边界
- FastAPI 是 async,但 `model.generate()` 是同步,必须 `await asyncio.to_thread(model.generate, ...)` 包一层。
- **不得用线程池**,会阻塞 FastAPI 事件循环。
- 并行模式用 `asyncio.gather + asyncio.wait_for`。

### R9. 配置外置
- 阈值 / 提示词 / 安全规则全部放 `config/*.yaml`,**不得硬编码到 `.py`**。
- 答辩时"为什么这么设计提示词"可以现场打开 YAML。

### R10. `inference_meta` 要存全
`{rank, router_weights, elapsed_ms, …}` 每次推理都写入 message 表。这是 4.5.6 案例分析与专家分化可视化的**唯一数据源**,漏存无法补。

### R11. 安全降级不是拒绝
替换风险表述 + 保留非风险结构 + 追加强化免责声明,三步缺一不可。免责声明用固定模板字符串,**不走 LLM**。

---

## 三、代码风格约定

| 项 | 约定 |
| --- | --- |
| 语言 | Python 3.10+,类型注解覆盖公共 API |
| 数据契约 | Pydantic v2,定义在 `app/schemas/` |
| 异步 | async def + `asyncio`;禁止 `threading` 做并发推理 |
| 日志 | `app.utils.logger.get_logger(name)` 输出 JSON 行 |
| 错误处理 | 系统边界校验输入;内部函数不加无意义的 try/except |
| 未实现 | `raise NotImplementedError("Phase N")` 并在 docstring 中写明阶段 |
| 注释 | 用简体中文;论文公式对应处用 `# 论文 4.x.y` 标注 |
| 测试 | `pytest`;异步测试加 `pytest-asyncio` |
| 配置加载 | 通过 `yaml.safe_load` 读 `config/*.yaml`,**不要用 `yaml.load`** |

---

## 四、环境与工具链

- **Python 环境**:conda env `qwen`(用户自备,已含部分依赖)。本仓库**只维护 `requirements.txt`**,**不执行 `pip install`**;需要补依赖时由用户手动安装。
- **依赖安装(用户按需执行)**
  ```bash
  conda activate qwen
  pip install -r requirements.txt
  ```
- **嵌入模型**:默认 `BAAI/bge-base-zh-v1.5`。首次使用需联网下载 ~400MB。若无外网,把 `config/settings.yaml` 的 `embedder.model_name_or_path` 改成本地目录路径。
- **FAISS 索引**:`data/faiss_index/{faiss.index, docs.jsonl}`,被 `.gitignore` 忽略,运行 `scripts/build_kb_index.py` 生成。
- **SQLite**:`data/consultation.db`,被 `.gitignore` 忽略,运行 `scripts/init_db.py` 生成。

---

## 五、阶段进度表

> 勾选法:`[x]` = 已完成,`[~]` = 进行中,`[ ]` = 未开始。每次推进任务后**务必**回来勾选对应条目并在页末"变更日志"追加一行。

### 阶段 0:基础设施(骨架 + mock 模型 + 数据库)

- [x] 项目结构与空 `__init__.py` 全部建出
- [x] 未实现模块的 `NotImplementedError` 占位签名
- [x] `requirements.txt`
- [x] `.gitignore`
- [x] `README.md`
- [x] `config/settings.yaml`(阈值 / rank_bins / embedder / retriever / timeouts)
- [x] `config/departments.yaml`(internal / surgery / pediatrics / general)
- [x] `config/prompts/`(base_template.txt + 四科室 YAML)
- [x] `config/safety_rules.yaml`(三类风险 + 免责模板)
- [x] `app/schemas/`(`CaseSummary / DeptCandidate / RoutingResult / DepartmentOpinion / FinalReport`)
- [x] `app/utils/logger.py`(JSON 行 stdout)
- [x] `app/utils/timer.py`
- [x] `app/utils/parser.py`(五字段正则 + completeness_score)
- [x] `app/storage/db.py`(SQLite 连接 + schema + 互斥锁)
- [x] `app/storage/{session_repo,case_repo,message_repo}.py`
- [x] `scripts/init_db.py`
- [x] `app/model/inference.py`(mock ModelEngine)
- [x] `app/model/moe_lora_loader.py`(Phase 7 占位)

### 阶段 1:知识库 + 路由调度器

- [x] `data/knowledge_base/{internal,surgery,pediatrics,general}/*.txt` 占位语料(每科室 2~3 篇)
- [x] `app/knowledge/kb_loader.py`(段落切分)
- [x] `app/knowledge/embedder.py`(BGE + sentence-transformers/transformers 双后端 + 单例)
- [x] `app/knowledge/retriever.py`(FaissRetriever + IndexFlatIP + save/load)
- [x] `scripts/build_kb_index.py`
- [x] `app/router/department_router.py`(max 聚合 + softmax + triage_tag)
- [x] `app/router/fallback.py`
- [x] `tests/test_router.py`(3 条科室归属明确用例 + fallback 校验)
- [x] **用户手动执行验证**:`python scripts/init_db.py` + `python scripts/build_kb_index.py` + `pytest tests/test_router.py -v`(4 passed)

### 阶段 2:专家智能体 + 会诊模式

- [x] `app/agents/base_agent.py` 实现 prompt 装配 + 输出解析流水(读 base_template + 科室 YAML,两步过滤,`asyncio.to_thread` 调模型,`parse_opinion_text` → `DepartmentOpinion`)
- [x] `app/agents/department_agent.py` 实现 `analyze()`(计时 + 结构化日志 + `agent_elapsed_ms` 写入 `inference_meta`)
- [x] `app/modes/mode_selector.py` 三模式选择(single_clear→serial / multi_cross→hybrid 或 parallel / ambiguous→parallel)
- [x] `app/modes/parallel.py`(`asyncio.gather + wait_for`,超时/异常视 absent)
- [x] `app/modes/serial.py`(前序意见压缩后作为 `context` 喂给下一个 agent)
- [x] `app/modes/hybrid.py`(核心组并发;弱意见占比 ≥1/3 才激活辅助组)
- [x] 三模式流程单测 `tests/test_modes.py` + agent 单测 `tests/test_agents.py`(12 passed)
- [x] `pytest.ini`(`asyncio_mode=auto`)

### 阶段 3:意见综合 + 安全审查

- [x] `app/aggregator/level1_consistency.py` 余弦最小值 (非均值,R6)
- [x] `app/aggregator/level2_weighted.py` 三因子加权 + `compute_weights` 调用接口
- [x] `app/aggregator/level3_arbitration.py` 报告组装(**不**调模型,R5)
- [x] `app/aggregator/pipeline.py` L1→L2→L3 递进 + `coordinator_hook` 回调签名,阈值 `tau_arbitrate`
- [x] `app/safety/rule_engine.py`、`degrader.py`、`disclaimer.py`(R11 三步:替换 + 保留结构 + 免责声明)
- [x] `app/agents/safety_agent.py` 编排 rule_engine → degrader → disclaimer
- [x] `tests/test_aggregator.py`(8) + `tests/test_safety.py`(5),25 passed

### 阶段 4:会诊协调器 + FastAPI(未开始)

- [ ] `app/coordinator/orchestrator.py` 线性调用链路
- [ ] `_arbitrate` 回调:以 `rank_hint=max(rank_bins)` 调模型
- [ ] `app/main.py` 四个 REST 接口 + `lifespan` 加载模型/FAISS
- [ ] 端到端 mock 模型测试(含冲突场景验证仲裁回调)

### 阶段 5:状态机 + 任务队列 + 超时容错(未开始)

- [ ] `app/coordinator/state_machine.py` 六状态 + 转移
- [ ] `app/coordinator/task_queue.py` 三级超时容错
- [ ] orchestrator 重构为状态机驱动

### 阶段 6:Streamlit 前端(未开始)

- [ ] `frontend/app.py`
- [ ] `frontend/pages/{1_病例录入,2_分诊结果,3_各科室意见,4_综合报告,5_历史记录}.py`
- [ ] `frontend/api_client.py`
- [ ] `scripts/run_dev.sh` 完善

### 阶段 7:接入真实模型(未开始)

- [ ] `app/model/moe_lora_loader.py`
- [ ] `ModelEngine._real_generate` + `_estimate_rank`(基座前向 → CE 均值 → 归一化 → 档位)
- [ ] 真实模型下端到端跑通,截 4.4 节用图

### 阶段 8:测试评估(未开始)

- [ ] `tests/test_functional.py` 五类用例
- [ ] `tests/test_performance.py` 30 次时延 + 50 次稳定性
- [ ] `tests/compare_baselines.py` 单模型 / 固定并行 / 固定串行
- [ ] `tests/ablation.py`(开关:`USE_ROUTER / USE_HYBRID_MODE / USE_SAFETY`,env var 控制)
- [ ] `tests/case_analysis.py` 可视化 JSON

---

## 六、下一步计划(给新会话的指令)

**当前状态**:阶段 0 + 1 + 2 + 3 代码与单测均已落地通过。**下一迭代目标**:阶段 4(会诊协调器 + FastAPI)。

### 6.1 阶段 3 已就绪,阶段 4 的入口

阶段 3 新增 `tests/test_aggregator.py` + `tests/test_safety.py` 共13 条用例,全部不依赖 FAISS/嵌入模型,运行方式:

```bash
conda activate qwen
python -m pytest tests/test_agents.py tests/test_modes.py tests/test_aggregator.py tests/test_safety.py -v   # 25 passed
python -m pytest tests/test_router.py -v                                                                    # 4 passed (依赖 faiss_index)
```

### 6.2 下一迭代建议(阶段 4)

按阶段 4 勾选项实现会诊协调器与 FastAPI 入口。优先级:

1. `app/coordinator/orchestrator.py` 线性调用链(router → 选 mode → run agents → aggregator → safety),每一步的产出写入 `message_repo`(R2:DB 只由协调器读写)。
2. `_arbitrate` 回调:`await asyncio.to_thread(model.generate, prompt, rank_hint=max(rank_bins))` —— 必须显式出现 `rank_hint=max(...)`(R5)。
3. `app/main.py` 四个 REST 接口 + `lifespan` 加载 `Embedder / FaissRetriever / ModelEngine / DepartmentRouter / AggregationPipeline / SafetyAgent`。
4. 端到端 mock 模型测试(含冲突场景验证仲裁回调 + `inference_meta` 全量落盘,R10)。

### 6.3 触碰本文件的时机

**每次**完成勾选项或出现架构决策调整,必须:
1. 在"阶段进度表"勾选 `[x]` 或切换 `[~]`
2. 在"变更日志"末尾追加 `YYYY-MM-DD: 简述` 一行
3. 若引入新约定,写入"硬性开发规范"或"代码风格约定"

---

## 七、关键契约速查

### 7.1 数据契约(Pydantic v2,见 `app/schemas/`)

| 类 | 用途 | 关键字段 |
| --- | --- | --- |
| `CaseSummary` | 病例输入 | `case_id / chief_complaint / symptoms / medical_history / exam_results` |
| `DeptCandidate` | 路由候选项 | `dept / confidence` |
| `RoutingResult` | 路由输出 | `candidates / triage_tag(single_clear/multi_cross/ambiguous) / fallback_triggered / retrieval_hits` |
| `DepartmentOpinion` | 科室意见 | 五字段 + `self_confidence(high/medium/low)` + `inference_meta` |
| `FinalReport` | 最终报告 | `summary / dept_opinions / aggregation_level(1-3) / safety_action(pass/degraded/arbitrated) / disclaimer` |

### 7.2 模型接口

```python
ModelEngine(checkpoint_path="", rank_bins=(8,16,24,32), default_rank=16, mock=True)
text, meta = model.generate(prompt, rank_hint=None, max_new_tokens=1024)
# meta = {"rank", "router_weights", "elapsed_ms", "mock"}
```

### 7.3 FastAPI 端点(阶段 4 实现)

```
POST /api/v1/session                      → {session_id}
POST /api/v1/consultation                 → FinalReport
GET  /api/v1/session/{sid}                → List[Message]
GET  /api/v1/session/{sid}/trace/{round}  → inference_meta trace
```

---

## 八、变更日志

- 2026-04-17: 初始化仓库, 完成阶段 0 + 阶段 1 全部代码, 创建本规范文件。占位知识库共 10 篇 .txt。骨架通过 `python -m compileall` 语法校验。
- 2026-04-17: 执行验证通过。`init_db` 建表成功, `build_kb_index` 生成 32 chunks(4 科室), `pytest tests/test_router.py` 4 passed。阶段 0 + 1 全部完成。
- 2026-04-17: 完成阶段 2。实现 `BaseAgent` 公共流水(模板加载 / 两步过滤 / prompt 装配 / `asyncio.to_thread` 推理 / 五字段解析)、`DepartmentAgent.analyze`、`mode_selector` 三模式分派、`parallel/serial/hybrid` 三模式执行器。新增 `tests/test_agents.py` + `tests/test_modes.py`(12 passed)与 `pytest.ini`(`asyncio_mode=auto`)。
- 2026-04-17: 完成阶段 3。实现三级意见综合 `AggregationPipeline` (L1 余弦最小值 / L2 三因子加权 / L3 `coordinator_hook` 回调)与安全审查 `SafetyAgent` (rule_engine → degrader → disclaimer 三步降级)。新增 `tests/test_aggregator.py` + `tests/test_safety.py` (合计 13 用例 passed,累计非路由类用例 25 passed)。
